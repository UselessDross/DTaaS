port: "4001"
mode: "local" #git or local
# local-path: "/dtaas/libms/files" # concatinate line 13, 16 and 19 respectivly, but not together on one line.
local-path: "C:/Education/Bachelor/BachelorWorkRepo/DTaaS/files"
log-level: "debug"
apollo-path: "/lib"
graphql-playground: "true"
# add a feild where the user can chose the interval for the autoSync
# possiblenames for the filed:


git-repos: #only used in git mod
        - user1:
                  repo-url: "https://gitlab.com/dtaas/user1.git"
                  #   http-token: "xxxx"
                  sync-interval: 60 # in seconds
        - user2:
                  repo-url: "https://gitlab.com/dtaas/user2.git"
                  #   http-token: "xxxx"
                  sync-interval: 50 # in seconds
        - common:
                  repo-url: "https://gitlab.com/dtaas/common.git"
                  #   http-token: "xxxx"
# dismantly the auto-sync folder and directly hard code the feature into the git-file and implement it into the git-module.
# lets make sure it is working and the testing.
# atomize the auto-sinc into different functions/classes that focuses on one of the features. one for git pull, one for git commit, and one for git push. and one for periodic actions.
